<view class="container">
  <view class="analysis-card" wx:for="{{recommendations}}" wx:key="aspect">
    <view class="card-title">{{item.aspect}}</view>
    <view class="card-content">{{item.content}}</view>
  </view>
</view> 